# Plan: Create Screen 2 (Interactive AI Timeline screen) with animated agent logs

```yaml
wave: 2
depends_on: [02-01]
files_modified:
  - src/app/timeline.tsx
autonomous: true
```

## Objective
Implement Screen 2 (`src/app/timeline.tsx`) with a high-fidelity interactive timeline. It will display the live agent execution steps, show rolling progress logs, highlight the active agent node with glowing animations, and provide control buttons to play/pause/reset the simulation loop.

## Tasks
<task id="TIMELINE_UI_LAYOUT">
  <read_first>
    - src/app/timeline.tsx
    - src/constants/theme.ts
  </read_first>
  <action>
    Build the timeline UI in `src/app/timeline.tsx`:
    - Display header showing the dataset name currently being operated on.
    - Render a vertical progress tracker representing the 5 agents (Intake, Insight, Root Cause, Strategy, Simulation, Evaluation).
    - Active steps should glow with a teal/cyan theme background. Completed steps should show a green check mark.
  </action>
  <acceptance_criteria>
    - `src/app/timeline.tsx` displays the 5 stages visually in a vertical or horizontal track.
  </acceptance_criteria>
</task>

<task id="TIMELINE_LOG_CONSOLE">
  <read_first>
    - src/app/timeline.tsx
  </read_first>
  <action>
    Integrate a terminal-style rolling log console at the bottom of Screen 2:
    - Lists the latest log events from the Convex run logs array.
    - Automatically scrolls to the bottom when new logs arrive.
    - Style logs according to their type: `info` (slate), `success` (teal), `warn` (orange), `error` (red).
  </action>
  <acceptance_criteria>
    - Terminal-style list component renders the log messages.
  </acceptance_criteria>
</task>

<task id="CONVEX_SIMULATION_INTEGRATION">
  <read_first>
    - src/app/timeline.tsx
    - convex/runs.ts
  </read_first>
  <action>
    Wire up UI controls:
    - Subscribe to real-time run updates using the `useQuery` hook.
    - If the run is completed, display a prominent button saying "Proceed to Dashboard" routing the user to `/dashboard`.
    - Provide a "Restart Loop" button that resets the run status and starts the simulation loop again.
  </action>
  <acceptance_criteria>
    - Click handlers exist for starting/restarting the simulation.
    - "Proceed to Dashboard" button appears when the run finishes and redirects the user to `/dashboard`.
  </acceptance_criteria>
</task>

## Goal-Backward Verification
- **must_haves**:
  1. The timeline page renders correctly with real-time log lists.
  2. Active agent state triggers glowing transition styles.
  3. Clicking restart successfully updates the run status in Convex.
  4. Once simulation is complete, the dashboard navigation button displays.
