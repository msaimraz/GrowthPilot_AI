# Plan: Create Screen 1 (File Upload & Demo Selector) with upload layout and demo cards

```yaml
wave: 2
depends_on: [01-02]
files_modified:
  - src/components/app-tabs.tsx
  - src/app/index.tsx
  - src/app/timeline.tsx
  - src/app/dashboard.tsx
  - src/app/actions.tsx
  - src/app/simulation.tsx
  - src/app/results.tsx
autonomous: true
```

## Objective
Update navigation routes to support the full 6-screen multi-agent flow, build the primary Intake screen with CSV/PDF drag-and-drop zones, add demo dataset selector cards, and wire up the file loading state to Convex.

## Tasks
<task id="UPDATE_NAVIGATION_TABS">
  <read_first>
    - src/components/app-tabs.tsx
  </read_first>
  <action>
    Modify `src/components/app-tabs.tsx` to include triggers for:
    - `index` (Upload / Intake)
    - `timeline` (Agent timeline)
    - `dashboard` (Insights dashboard)
    - `actions` (Recommendations)
    - `simulation` (Action simulation)
    - `results` (Results / Impact)
    Ensure they render clean labels and icons appropriate for a dark UI theme.
  </action>
  <acceptance_criteria>
    - `src/components/app-tabs.tsx` contains trigger tags for `index`, `timeline`, `dashboard`, `actions`, `simulation`, and `results`.
  </acceptance_criteria>
</task>

<task id="CREATE_ROUTE_PLACEHOLDERS">
  <read_first>
    - src/app/index.tsx
  </read_first>
  <action>
    Create placeholder route files under `src/app/`:
    - `src/app/timeline.tsx`
    - `src/app/dashboard.tsx`
    - `src/app/actions.tsx`
    - `src/app/simulation.tsx`
    - `src/app/results.tsx`
    Each should render a basic themed layout with title and "Under Construction" text to ensure routing is error-free.
  </action>
  <acceptance_criteria>
    - Files `timeline.tsx`, `dashboard.tsx`, `actions.tsx`, `simulation.tsx`, `results.tsx` exist under `src/app/`.
  </acceptance_criteria>
</task>

<task id="IMPLEMENT_INTAKE_SCREEN">
  <read_first>
    - src/app/index.tsx
    - src/constants/theme.ts
  </read_first>
  <action>
    Implement a premium dark-themed file upload interface in `src/app/index.tsx`:
    - Display header title "GrowthPilot AI" and subtitle.
    - Create a drop-zone style container for uploading PDF/CSV files.
    - Provide a "Select Demo Dataset" section containing cards for predefined scenarios (e.g., "Shopify + Meta Ads Anomalies").
    - Add interactive animations or status indicators (e.g. ActivityIndicator or animated bars) showing when a file is processing.
  </action>
  <acceptance_criteria>
    - `src/app/index.tsx` contains code for the file drop zone styling and a list of demo datasets.
    - File upload uses standard react-native touch handlers or picker hooks.
  </acceptance_criteria>
</task>

<task id="WIRE_INTAKE_TO_CONVEX">
  <read_first>
    - src/app/index.tsx
    - convex/runs.ts
  </read_first>
  <action>
    Connect the Intake screen to Convex:
    - Import `useMutation` from `convex/react`.
    - When a user uploads a file or selects a demo dataset, trigger the Convex mutation to create a new agent run with initial state (status: "Observe", progress logs initialized).
    - Upon success, navigate the user to the timeline screen (`/timeline`).
  </action>
  <acceptance_criteria>
    - `src/app/index.tsx` uses Convex `useMutation` hook to create a run record.
    - Successful submission triggers redirection to `/timeline` using `router.push("/timeline")` or similar from `expo-router`.
  </acceptance_criteria>
</task>

## Goal-Backward Verification
- **must_haves**:
  1. The app menu bar displays all six tabs in correct sequence.
  2. Tab navigation works without crashes when navigating between empty pages.
  3. Clicking "Demo Dataset" successfully runs the Convex mutation, inserts a run record, and routes to `/timeline`.
