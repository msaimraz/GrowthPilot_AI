# Plan: Initialize React Native Expo app styling, theme and fonts

```yaml
wave: 1
depends_on: []
files_modified:
  - src/constants/theme.ts
  - src/app/_layout.tsx
  - src/global.css
autonomous: true
```

## Objective
Configure core app appearance, load Outfit or Inter fonts, set up the dark mode color palette (slate, teal, cyan), and prepare the styling system.

## Tasks
<task id="THEME_SETUP">
  <read_first>
    - src/constants/theme.ts
    - src/global.css
  </read_first>
  <action>
    Modify `src/constants/theme.ts` to define the premium dark mode color palette:
    - Slate background: `#0F172A`
    - Charcoal elements: `#1E293B`
    - Teal/cyan accents: `#0EA5E9` and `#14B8A6`
    - High contrast text: `#F8FAFC` and secondary text `#94A3B8`
    Ensure typography specifies 'Outfit' or 'Inter' as fonts.
  </action>
  <acceptance_criteria>
    - `src/constants/theme.ts` contains the hex colors `#0F172A`, `#1E293B`, `#0EA5E9`, and `#14B8A6`.
  </acceptance_criteria>
</task>

<task id="GLOBAL_CSS_SETUP">
  <read_first>
    - src/global.css
  </read_first>
  <action>
    Update `src/global.css` to import Outfit font from google fonts or locally and define basic utility styling classes for dark mode:
    ```css
    @import url('https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;600;700&display=swap');
    :root {
      --font-display: 'Outfit', sans-serif;
    }
    ```
  </action>
  <acceptance_criteria>
    - `src/global.css` contains the import URL for 'Outfit'.
  </acceptance_criteria>
</task>

## Goal-Backward Verification
- **must_haves**:
  1. App compiles without TypeScript or font loading errors.
  2. Theme constants exported in `src/constants/theme.ts` match the specified premium hex values.
  3. `global.css` correctly imports the Outfit Google font.
