# Plan: Setup Convex backend, deploy schema, and connect real-time client

```yaml
wave: 1
depends_on: [01-01]
files_modified:
  - package.json
  - src/app/_layout.tsx
  - convex/schema.ts
  - convex/runs.ts
autonomous: true
```

## Objective
Install `convex` client library, define the initial database schema (uploads, runs tables), set up a simple transition mutation, and wrap the app in the `ConvexProvider` client.

## Tasks
<task id="INSTALL_CONVEX">
  <read_first>
    - package.json
  </read_first>
  <action>
    Install `convex` dependency to package.json.
    Run `npm install convex` at the project root to ensure node_modules are updated.
  </action>
  <acceptance_criteria>
    - `package.json` contains `"convex":` in its dependencies.
  </acceptance_criteria>
</task>

<task id="CONVEX_SCHEMA_SETUP">
  <read_first>
    - package.json
  </read_first>
  <action>
    Create a `convex/schema.ts` file that defines:
    - `uploads`: table for uploaded report files (metadata, original name, raw CSV/PDF parsed content).
    - `runs`: table tracking agent loops (Observe, Reason, Decide, Act, Evaluate status, active agent name, error logs, and computed outputs).
  </action>
  <acceptance_criteria>
    - File `convex/schema.ts` exists.
    - `convex/schema.ts` defines `uploads` and `runs` tables.
  </acceptance_criteria>
</task>

<task id="CONVEX_MUTATIONS_SETUP">
  <read_first>
    - convex/schema.ts
  </read_first>
  <action>
    Create a `convex/runs.ts` file with mutations and queries to:
    - Create a new run when a report is uploaded or demo selected.
    - Retrieve run status by ID.
    - Update run status (e.g., transition state, append agent execution logs).
  </action>
  <acceptance_criteria>
    - File `convex/runs.ts` exists.
    - `convex/runs.ts` defines `create`, `get`, and `update` mutations/queries.
  </acceptance_criteria>
</task>

<task id="CONVEX_PROVIDER_INTEGRATION">
  <read_first>
    - src/app/_layout.tsx
  </read_first>
  <action>
    Modify `src/app/_layout.tsx` to:
    - Import `ConvexProvider` and `ConvexReactClient` from `convex/react`.
    - Initialize the Convex client with a URL (e.g., environment variable or fallback string `process.env.EXPO_PUBLIC_CONVEX_URL ?? "http://localhost:5173"`).
    - Wrap the app's components with `ConvexProvider`.
  </action>
  <acceptance_criteria>
    - `src/app/_layout.tsx` imports `ConvexProvider` and `ConvexReactClient`.
    - `src/app/_layout.tsx` wraps `AppTabs` or its child tree with `ConvexProvider`.
  </acceptance_criteria>
</task>

## Goal-Backward Verification
- **must_haves**:
  1. `convex` package is correctly installed.
  2. `convex/schema.ts` compiles successfully.
  3. App layout exports a client wrapped in `ConvexProvider`.
  4. Realtime Convex queries can be run without errors.
