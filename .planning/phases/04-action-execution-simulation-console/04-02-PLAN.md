# Plan: Build Screen 5 (Action Simulation Console) with editable forms and interactive log console

```yaml
wave: 2
depends_on: [04-01]
files_modified:
  - src/app/simulation.tsx
autonomous: true
```

## Objective
Build Screen 5 (`src/app/simulation.tsx`) to provide an interactive simulation console. This screen will read the `simulationLogs` from the active Convex run and display them in a live terminal-style view. It will also provide a call-to-action to proceed to the Evaluation results.

## Tasks
<task id="BUILD_SIMULATION_CONSOLE_UI">
  <read_first>
    - src/app/simulation.tsx
  </read_first>
  <action>
    Create a high-fidelity responsive layout for `simulation.tsx`:
    - Display a header indicating "ACTION SIMULATION CONSOLE".
    - Render a dark-themed terminal container to show live simulation logs with color-coded status indicators (success, error, etc.).
    - Display an animated processing state if the simulation is ongoing.
    - Provide a CTA button "View Evaluation Results →" to navigate to `/results` when the simulation is complete or logs are available.
  </action>
  <acceptance_criteria>
    - The simulation console displays the mocked execution logs fetched from Convex.
    - Terminal UI styling matches the premium dark mode aesthetic.
    - The screen compiles without TypeScript errors.
  </acceptance_criteria>
</task>

## Goal-Backward Verification
- **must_haves**:
  1. Screen compiles with zero TS errors.
  2. Data is reactively fetched from Convex.
