# Plan: Implement Evaluation Agent outcomes projections

```yaml
wave: 1
depends_on: []
files_modified:
  - convex/runs.ts
autonomous: true
```

## Objective
Acknowledge and verify the backend evaluation data structure implemented in Phase 2's `simulateLoop` action. The payload provides before-and-after metric states that the frontend will render.

## Tasks
<task id="VERIFY_EVALUATION_PAYLOADS">
  <read_first>
    - convex/runs.ts
  </read_first>
  <action>
    Verify that the `saveEvaluationResult` helper is correctly populating the `evaluation` field on the run document with arrays of metric objects detailing `metric`, `before`, `after`, and `change`.
  </action>
  <acceptance_criteria>
    - The `evaluation` field structure is confirmed and ready for frontend integration.
  </acceptance_criteria>
</task>

## Goal-Backward Verification
- **must_haves**:
  1. The evaluation object structure is confirmed for frontend mapping.
