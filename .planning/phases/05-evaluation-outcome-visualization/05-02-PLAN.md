# Plan: Create Screen 6 (Evaluation Results) with outcome charts, verify end-to-end flow

```yaml
wave: 2
depends_on: [05-01]
files_modified:
  - src/app/results.tsx
autonomous: true
```

## Objective
Build Screen 6 (`src/app/results.tsx`) to display the final business outcome projections.

## Tasks
<task id="BUILD_RESULTS_UI">
  <read_first>
    - src/app/results.tsx
  </read_first>
  <action>
    Create a high-fidelity responsive layout for `results.tsx`:
    - Display an "EVALUATION ENGINE — OUTCOME" header.
    - Implement a summary card detailing the overarching conclusion.
    - Build a "Before vs After" metric comparison list, potentially using simple CSS-based visual bars to represent the change delta.
    - Render a prominent CTA button that navigates the user back to the home page (`/`) to test a new dataset.
  </action>
  <acceptance_criteria>
    - The results screen successfully maps the `evaluation` data from Convex.
    - The UI presents a clean, premium visual summary of the projected improvements.
    - The CTA resets the flow effectively.
  </acceptance_criteria>
</task>

## Goal-Backward Verification
- **must_haves**:
  1. Screen compiles with zero TS errors.
  2. Data is displayed clearly and accurately.
